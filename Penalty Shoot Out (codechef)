#include <bits/stdc++.h>
using namespace std;

int main() {
 int T;
    cin >> T;
    while (T--) {
        int X, Y;
        cin >> X >> Y;
        if (X + 2 >= Y && Y + 1 >= X) {
            cout << "YES" << endl;
        } else {
            cout << "NO" << endl;
        }
    }
    return 0;
}
